function closest = find4closest(image, g)
%FIND4CLOSEST finds the four closest circles to the center of an image 
%must be a grid of circles

[gw, gh, ~] = size(g); %get width and height of grid
[iw, ih] = size(image); %get width and height of image

imcenter = 

%% find closest circle






end

